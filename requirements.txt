# Dépendances de l'APPLICATION uniquement.
# C'est ce fichier qu'installe Streamlit Community Cloud : le garder minimal
# accélère le déploiement et réduit la surface d'échec.
# L'app ne lit que des CSV — ni parquet, ni scipy, ni yfinance au runtime.
streamlit>=1.30
pandas>=2.0
numpy
plotly
